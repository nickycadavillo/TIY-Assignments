// Create an object to hold information on your fav recipe.
//
// It should have properties for title (string), servings (number), and ingredients (array of strings).
//
// One separate lines (one console.log statement for each), log the recipe information so that is looks like:



var recipe = {
    title: "mole",
    serves: "serves: " + 2,
    ingredients: ["cinnamon", "cumin", "cocoa"]
}


console.log(recipe.title);
console.log(recipe.serves);
console.log(recipe.ingredients);
