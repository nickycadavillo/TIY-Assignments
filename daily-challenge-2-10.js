// Write a function named calculateAge that:
// Takes 2 arguments: birth year, current year
// calculates the 2 possible ages based on those years
// outputs the results to the screen like so: "You are either NN or NN"
// Call the function 3 times with different sets of values

//calculateAge
//calculateAge - 1

function calculateAge (birth year, current year) {

}


console.log("You are either " + "calculateAge" + " or" + "calculateAge - 1")
