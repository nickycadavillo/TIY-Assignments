// write a function named helloWorld that:
// takes 1 argument, a language code (es, de, en)
// returns "hello, world" for the given language, for at least 3 languages.
// It should default to returning english
// call that function for each of the support languages and log the result to make sure it works

function helloWorld (nn){
    if (nn === es) {
        return "hola, mundo";
    }
    else if (nn === fr) {
        return "bonjour, monde";

    } else   (nn === en) {
        return  "hellow, world";

    }
};

console.log(hellowWorld(es));

//syntax errors!!!
