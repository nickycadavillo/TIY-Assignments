//write a function that takes a certain type of tag and counts the number of elements with that tag.
// the function should return "there are x tags of type y on the page"

//example:  countTags('p'): "there are 3 tags of type p on the page"
//


function countTags(p) {
   var y = p.length // whoops no time and i shouldn't of used this!
    console.log("There are" + y + "tags of type" + p "on the page");
}

console.log(countTags());



function countTags(p) {
   for (p = 0; p < p.length; p++ )
}

console.log(countTags());
